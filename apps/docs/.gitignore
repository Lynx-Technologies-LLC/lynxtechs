# Dependencies
/node_modules

# Production build
/build

# Docusaurus generated files
.docusaurus
.cache-loader

# Build-time content synced from lxmaster / lxmaster-demos (see scripts/).
# docs/lxmaster/ is populated at build time from the latest versioned docs by
# prebuild.mjs and must not be committed (it is a derived copy, not a source).
/docs/lxmaster

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
